
import 'package:flutter/material.dart';
class UnpaidPage extends StatelessWidget {
  const UnpaidPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Unpaid'),);
  }
}